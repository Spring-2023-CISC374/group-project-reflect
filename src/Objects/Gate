import Phaser from "phaser";

export default class Gate extends Phaser.GameObjects.Sprite{
    constructor(scene: Phaser.Scene, x: number, y: number) {
        super(scene, x, y, 'BowlingBall');
        scene.add.existing(this);
    }

}